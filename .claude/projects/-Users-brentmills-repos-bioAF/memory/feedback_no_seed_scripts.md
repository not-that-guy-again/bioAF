---
name: No mock seed scripts
description: User no longer uses mock/demo seed data -- working with real data only
type: feedback
---

Do not create or maintain seed scripts with mock/demo data. The old `scripts/seed_poc_data.py` was removed. The user works with real data now.

**Why:** The project has moved past the demo/POC phase.
**How to apply:** Never generate mock seed scripts or reference demo accounts (maria@bioaf-demo.org, etc.) when setting up data.
